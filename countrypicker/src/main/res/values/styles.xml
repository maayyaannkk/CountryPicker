<resources>

    <!-- Base application theme. -->
    <style name="CountryAppTheme" parent="Theme.AppCompat.Light.Dialog.MinWidth">
        <item name="windowNoTitle">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>

</resources>
